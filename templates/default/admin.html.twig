{% extends 'base.html.twig' %}

{% block page_stylesheets %}

{% endblock %}

{% block page_scripts %}

{% endblock %}

{% block content %}
  <div class="container">
    <div class="row">
      <div class="col-sm-5 offset-sm-2">
        <h1>Dataset Administration</h1>
        <div class="spacer25"></div>

        <ul>
          <li>
            <a href="{{ path('approval_queue') }}">View Unpublished Datasets</a>
          </li>
          <li>
            <a href="{{ path('add_dataset', { entityName: 'Dataset' }) }}">Add a New Dataset</a>
          </li>
          <li>
            <a href="{{ path('update_entity', { entityName: 'Dataset' }) }}">Edit an Existing Dataset</a>
          </li>
          <li class="mt-4">
            <a href="{{ path('remove_entity', { entityName: 'Dataset' }) }}">Remove a Dataset</a>
          </li>
        </ul>
      </div>
    </div>
  </div>
{% endblock %}
